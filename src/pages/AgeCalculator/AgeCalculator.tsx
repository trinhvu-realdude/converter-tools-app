import { NavBar } from "../../components/NavBar/NavBar";

const AgeCalculator = () => {
    return (
        <>
            <NavBar title="Age" />
        </>
    );
};

export default AgeCalculator;
